{
  "name": "bridgecast-mobile",
  "version": "1.0.0",
  "description": "BridgeCast — Audio/Video Bridge & Remote Control native mobile app",
  "scripts": {
    "sync":    "npx cap sync",
    "android": "npx cap open android",
    "ios":     "npx cap open ios",
    "run:android": "npx cap run android",
    "run:ios":     "npx cap run ios"
  },
  "dependencies": {
    "@capacitor/android":                     "^6.0.0",
    "@capacitor/core":                        "^6.0.0",
    "@capacitor/ios":                         "^6.0.0",
    "@capacitor-community/background-mode":   "^0.3.1",
    "@capacitor-community/keep-awake":        "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
